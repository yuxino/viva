import Note from "./Note"

# MDX, without hidden execution

Viva keeps MDX source readable while treating components and expressions as
untrusted text. Ordinary Markdown around them still keeps its rhythm.

<Note tone="quiet">
  This component stays visible as source and never runs inside the editor.
</Note>

{draftStatus}
